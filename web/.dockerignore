# Dependencies (rebuilt inside container)
node_modules/
# NOTE: dist/ is NOT excluded here — Dockerfile.local copies pre-built dist into nginx

# Test files not needed in production image
*.test.ts
*.test.tsx
__tests__/
src/test/
